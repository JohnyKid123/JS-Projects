const decimalNum = parseInt(prompt("Enter a decimal number: "));
const hexNum = decimalNum.toString(16).toUpperCase();
const binaryNum = decimalNum.toString(2);
console.log(hexNum);
console.log(binaryNum);
